/* 全局样式 */
page {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-size: 28rpx;
  color: #1f2937;
  background: #f0f2f5;
}

button {
  background: none;
  padding: 0;
  margin: 0;
  line-height: 1.4;
}

button::after {
  border: none;
}